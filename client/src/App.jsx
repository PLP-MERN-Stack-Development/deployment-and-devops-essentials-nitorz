import React from 'react';

export default function App(){
  return (
    <div>
      <h1>Production-ready React App</h1>
      <p>Connects to API via <code>REACT_APP_API_URL</code></p>
    </div>
  );
}
