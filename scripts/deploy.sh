#!/usr/bin/env bash
# Example manual deploy script (customize per provider)
echo "This script is an example. Replace with provider-specific deploy steps."
echo "For Render/GitHub: configure automatic deploys in Render dashboard."
